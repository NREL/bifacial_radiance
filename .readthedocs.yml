version: 2

sphinx:
  # Path to your Sphinx configuration file.
  configuration: docs/sphinx/source/conf.py

build:
   os: ubuntu-20.04
   tools:
     python: "3.8"

python:
    install:
        - method: pip
          path: .
          extra_requirements:
              - doc